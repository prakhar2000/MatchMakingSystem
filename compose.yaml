version: "3.9"

services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: matchmaking
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  redis:
    image: redis:8
    ports:
      - "6379:6379"
